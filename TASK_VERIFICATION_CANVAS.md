# Task Verification Canvas

This file is the shared task ledger for ongoing work in `ai-mindset-blocks`.

Status meanings:
- `requested`: asked for, not yet implemented
- `implemented`: changed in code/content, not fully verified yet
- `self-checked`: assistant re-checked the result directly
- `user-approved`: explicitly approved by the user

## User Requests

| ID | Task | Owner | Status | Last Checked | Notes |
|---|---|---|---|---|---|
| U-019 | Swap the current main-site `cases` block with the latest compact experiment: move the experimental compact grid onto the main site and relocate the original live-site cases layout into `ai-mindset-blocks` | user | self-checked | 2026-03-27 | The latest compact cases pattern is now used on the main site, and `ai-mindset-blocks` includes a new `CURRENT LIVE SITE / RELOCATED` cases variant for the old live-site approach |
| U-001 | Add the FAQ block based on variant 5 to the end of the `ai-mindset-blocks` page before the footer area | user | self-checked | 2026-03-26 | Inserted as a bottom-of-page block outside the tab gallery so it is available in the main page flow |
| U-002 | Rename the inserted block heading to `вопросы и ответы` and remove `ver_5.0` from the header | user | self-checked | 2026-03-26 | Header now uses the new title and suppresses the version line while keeping the rest of the styling intact |
| U-003 | Add the `LABS NAVIGATOR` ecosystem block based on the third navigator variant to the lower page flow | user | self-checked | 2026-03-26 | Reused the existing third navigator component and placed it in the lower page flow without duplicating its source code |
| U-004 | Mirror the live-site `speakers`, `cases`, and `pricing` blocks into `ai-mindset-blocks`, add missing `cases` and `pricing` tabs, fix broken speaker photos, add a four-column always-open speakers version, create two clearer case variants, and make pricing fully expanded | user | self-checked | 2026-03-27 | Reworked after review: speaker images now use `BASE_URL`, added second reviews variant, and tightened `cases`/`pricing` toward the live-site grid, button style, and smaller radii |
| U-005 | Make the top examples toggle more obvious: clearly label the payment popups area and the section-switcher so it reads as navigation between different site elements/pages | user | self-checked | 2026-03-27 | Added a large catalog heading, explicit payment-popup explanation, and a clearer section-navigation block with helper copy and arrowed tab labels |
| U-006 | In `Program NEW` version 7, change weeks by scroll and keep the left `недели` column from jumping in height even if the main card content varies by week | user | self-checked | 2026-03-27 | Added wheel/trackpad week switching on the right panel and gave the left sidebar its own stable desktop height |
| U-007 | Make `reviews` clearly include the second version, reduce `speakers v3` to live-site scale, redraw `cases` illustrations in the requested light line/grid style, and shrink `pricing` further | user | self-checked | 2026-03-27 | Second reviews variant remains in place; `speakers v3`, `cases`, and `pricing` were all reworked again toward the live-site proportions and the provided visual references |
| U-008 | Bring the live-site reviews logic into the research app, replace the round grid-toggle with a stricter rectangular control, fix the broken expanded grid behavior, and try a mix of real photos plus voxel-style placeholders | user | self-checked | 2026-03-27 | Added a third reviews version with live-site open/close logic, a strict rectangular toggle, stable fixed-grid expanded mode, two real photos, and voxel placeholders for the remaining cards |
| U-009 | Add more reviews into the experimental transferred reviews block, replace the meaningless labels with useful identity/role information, and swap the menu-like three dots for a calmer technological ASCII accent | user | self-checked | 2026-03-27 | Expanded the reviews dataset, moved names into the black label, replaced the old label row with the person’s role, and swapped the dots for a keyboard-like accent |
| U-010 | Add a separate `voxel` page inside `ai-mindset-blocks` for logo work and rebuild the split logo so the right voxel half matches the real PNG proportions more closely | user | self-checked | 2026-03-27 | Added a dedicated `Воксель / Лого` tab with a standalone lab screen; left half stays PNG, right half is sampled into animated voxels from the same PNG silhouette |
| U-011 | Add denser voxel variants, reduce the seam gap, fix the awkward top gap and bottom tail shape, and introduce richer entry/scroll motion in the dedicated voxel-logo lab | user | self-checked | 2026-03-27 | Added two denser variants plus a refined base version; tightened the seam, improved top coverage and tail rounding, and introduced stronger initial assembly with optional scroll drift |
| U-012 | Add two much smaller `cases` variants: one with only the name plus one sentence, and one with stack tags styled as tags instead of buttons | user | self-checked | 2026-03-27 | Added two new compact cases grids: one without tool tags and one with subdued stack tags that read as metadata, not CTAs |
| U-013 | Duplicate the latest `speakers` version, remove the line between the speaker name block and description, and adjust the grid after review | user | self-checked | 2026-03-27 | Added a new speakers variant based on `V3`, kept the divider removed, and after review changed the latest variant from 3 columns to a 5-column desktop grid |
| U-014 | In the dedicated voxel-logo lab, remove the first two variants, keep only the third, pull the two halves 30–40% closer, and reduce voxel count by about 20% to remove lag | user | self-checked | 2026-03-27 | Reduced the voxel lab to one optimized variant, tightened the seam gap, lowered voxel count in the non-critical areas, and reduced animation cost |
| U-015 | Swap speakers between the live site and `ai-mindset-blocks`: move the current live-site speakers block into research as an extra version, and promote the new 5-column always-open version with bolded selling keywords to the main site | user | self-checked | 2026-03-27 | Research now includes the relocated current live-site speakers block as an extra version, while the new 5-column always-open speaker layout with bold emphasis is prepared for the main site |
| U-016 | Refine the right eye in the dedicated voxel-logo lab so it reads larger, cleaner, and closer to the original PNG instead of looking too small or torn | user | self-checked | 2026-03-27 | Removed the hand-drawn eye mask, switched the eye cutout to PNG dark-region sampling, and restored more density along the outer arc so the right half keeps a rounder silhouette near the eye |
| U-017 | Rework one compact `cases` variant so it is less monotone: add green hover like the live site, add more varied visual elements, and keep stack tags without the look of buttons | user | self-checked | 2026-03-27 | `DesktopCasesV4` now uses the live-site green hover, Russian case titles, and more varied signal/route/base previews in the site palette; stack tags remain visible but were flattened into metadata text instead of bordered chips |
| U-018 | In the `pricing` block experiments, make the cards read more clearly: stack vertically on mobile, replace oversized dots with smaller technical markers, move the plan labels into a single rectangular tag, remove redundant duplicate details, and change CTA text to `записаться` | user | self-checked | 2026-03-27 | `DesktopPricingV1` now stacks cards in one column on mobile, uses a single top-right tag (`БАЗА`, `+4 ЗАНЯТИЯ`, `ПЕРСОНАЛЬНОЕ СОПРОВОЖДЕНИЕ`), replaces large dots with subtle arrow markers, switches section labels to `программа` / `что получаешь`, removes duplicated premium points, and changes the CTA to `записаться` |
| U-030 | Tweak the right-eye voxel cutout so the gap shifts two columns left, removes the stray dot, and keeps the animation aligned with the reference eye shape | user | implemented | 2026-03-31 | Adjusted the eye mask sampling so the white hole is wider toward the left and the extra black dot no longer appears; canvas animation ready for verification |
| U-020 | Create a new `Program NEW` hybrid version: use the first style from `Программа до 24.03` as the behavioral base, avoid a long stacked list, keep it compact, and cross it with the darker mood from the animated version; also renumber the visible program version headings in the UI so they follow on-page order | user | implemented | 2026-03-29 | Still under revision after review: previous passes had duplicated information and mismatched numbering; numbering is now being moved into the continuous `Версия 12+` sequence |
| U-021 | Build several more `Program NEW` alternatives that use the same week-switching UX but reduce decorative duplication, make black areas functional, and explore different information architectures instead of one oversized dark visual panel | user | implemented | 2026-03-29 | Still under revision after review: black zones in several variants remained decorative or duplicated content instead of carrying distinct function |
| U-022 | Duplicate the chosen hybrid idea into two new versions where the top area keeps only week/date/theme/rhythm information, the lower area becomes three lecture cards (`Основной трек`, `Advanced`, `Лекция`), and the newest versions appear first in the list | user | implemented | 2026-03-29 | Still under revision after review: top variants remain visible first, but the user reported layout and styling mismatches that require another pass |
| U-023 | Duplicate the chosen split dark/light version into a new `Program NEW` variant with the week rail on the left plus scroll switching, remove duplicate week labels and decorative noise, remove the curator block, and place the week label above the main description instead | user | implemented | 2026-03-29 | New split version added at the top of `Program NEW`; left panel keeps the mood but drops duplicated `Неделя`, `signal`, `shift`, `live field`, and the right side removes the curator row |
| U-024 | In `program_main`, move the current visible `ВЕРСИЯ 17` and `ВЕРСИЯ 18` blocks up so they become `ВЕРСИЯ 15` and `ВЕРСИЯ 16` on the page | user | implemented | 2026-03-30 | Reordered the `program_main` stack so the former `17/18` variants now sit before the old `15/16`, which were renumbered down to `17/18` |
| U-025 | Refine `program_main` `ВЕРСИЯ 13`: make the bottom dark center card feel more like a designed signal panel instead of a flat black rectangle, remove rounded feel, swap the background treatment, and reduce the `Недельный ритм` block so it pulls less attention | user | implemented | 2026-03-30 | Calendar sizing change landed, but the user reported visual issues in the dark-card art direction; advanced-track typography and the protective text treatment are still being corrected |
| U-026 | Refine `program_main` `ВЕРСИЯ 14`: keep the whole lower zone black, reduce the rhythm block height sharply, keep `CONTEXT ENGINEERING` on one line in the lower card, and make all three dark cards read like the blurred neon poster reference without the art colliding with text | user | implemented | 2026-03-30 | Previous pass improved the card shell, but the user reported bottom clipping and not enough central blur overlap; this pass raises the cards closer to the rhythm block and strengthens the middle-card blur stack |
| U-027 | Refine `program_main` `ВЕРСИЯ 16`: reduce the visual mess on the left side of the dark hero so text stays readable, and shrink the white `Недельный ритм` block plus calendar to roughly the smaller scale used in `ВЕРСИЯ 13` | user | implemented | 2026-03-30 | `DesktopTechUiV15` now keeps the rhythm at the `ВЕРСИЯ 13` scale and explicitly hands the remaining fixed card height to the dark hero (`flex-1`) while the white rhythm shelf is `shrink-0`, so the empty lower white area should collapse instead of dominating the card |
| U-028 | Rebuild `program_main` `ВЕРСИЯ 14` into a two-column top block: keep the full weekly text on the left, move a narrower weekly rhythm to the right, and turn the lower black section into a labeled `Основные занятия недели` area where simpler lecture cards sit above a shared blurred signal background | user | implemented | 2026-03-30 | `DesktopTechUiV17` now uses separate row/column allocation for the description vs rhythm area, a narrower overall card shell, a more unified type system, wider rhythm width without squeezing the left text, more translucent lecture cards, and a safer responsive card grid that should avoid clipping speaker rows |
| U-029 | On the `speakers` page, duplicate version 2 and create a new variant where the speaker text always sits below the image, the image becomes a rounded 2:3 vertical portrait instead of a circle, and the card gets corner-frame accents like the reference | user | implemented | 2026-03-30 | Added a new speakers variant based on the round-grid logic, but with portrait-format images, always-visible text below, and corner marks around each card; visual review is still pending |

## Agent Self-Checks

| ID | Check | Owner | Status | Last Checked | Notes |
|---|---|---|---|---|---|
| A-019 | Relocate the original main-site `cases` layout into `ai-mindset-blocks` as a new reviewable variant and verify the research app still builds | assistant | self-checked | 2026-03-27 | Added `DesktopCasesOriginal`, wired it into the `cases` tab as a fifth variant, re-used shared preview primitives, and re-ran `npm run build` successfully |
| A-001 | Mount the new end-of-page FAQ block without disturbing the existing tab gallery behavior more than necessary | assistant | self-checked | 2026-03-26 | Reused `DesktopFaqV5` via props instead of duplicating the accordion markup |
| A-002 | Verify the modified `ai-mindset-blocks` app still builds and provide localhost/LAN preview links for review | assistant | self-checked | 2026-03-26 | `npm run build` passed and the live dev server remains available on `:4007` |
| A-003 | Add the lower-page `LABS NAVIGATOR` block without breaking the existing `navigator` tab and re-verify the app | assistant | self-checked | 2026-03-26 | Reused `DesktopMiniLabsNavigatorV2`, gated the new bottom instance to avoid a duplicate on the `navigator` tab, and re-ran `npm run build` successfully |
| A-004 | Port live-site content into the `ai-mindset-blocks` research app, add `cases` and `pricing` tabs, restore speaker photos, and verify the app still builds | assistant | self-checked | 2026-03-27 | Updated speaker asset URLs to use `import.meta.env.BASE_URL`, added `DesktopReviewsV2`, reduced radii and card sizes in `cases`/`pricing`, switched pricing CTAs to live-site button classes, and `npm run build` plus direct `curl` checks on `:4007` passed |
| A-005 | Clarify the top examples navigation so users can distinguish payment-popup examples from the cross-section site-element switcher | assistant | self-checked | 2026-03-27 | Updated `App.tsx` with explicit headings, helper descriptions, and stronger tab affordance; `npm run build` passed afterward |
| A-006 | Add scroll-driven week switching to `DesktopTechUiV7` and stop the left week rail from stretching with right-side content height changes | assistant | self-checked | 2026-03-27 | Added a wheel handler with bounded week changes, preserved click navigation, set the desktop left rail to a stable min-height, and `npm run build` passed |
| A-007 | Rework `speakers v3`, `cases` visuals, and `pricing` scale after review and verify the research app still builds | assistant | self-checked | 2026-03-27 | Reduced `DesktopSpeakersV3` to a simpler live-site-sized layout, rebuilt `ResearchCaseVisual` into a light grid/ASCII signal style, compressed `DesktopPricingV1` further, and `npm run build` passed |
| A-008 | Port the live-site reviews open/expand logic into a new research variant and fix the broken grid behavior while testing mixed avatar treatments | assistant | self-checked | 2026-03-27 | Added `DesktopReviewsV3`, switched the toggle to a rectangular control, used a stable equal-card expanded grid, downloaded real photos for Olga Eremina and Oleg Tzerbaev, generated voxel placeholders for the remaining cards, and verified `npm run build` plus local image URLs on `:4007` |
| A-009 | Refine `DesktopReviewsV3` after visual feedback so the expanded mode reads like a denser tile field with clearer identity markers and less fake-UI noise | assistant | self-checked | 2026-03-27 | Added three more reviews, switched the black chip to the reviewer name, replaced the meaningless tag row with the reviewer role, changed the top-right dots to an ASCII accent, and re-verified with `npm run build` |
| A-010 | Create a dedicated voxel-logo playground inside `ai-mindset-blocks`, use the real logo PNG as the geometry source for the animated right half, and verify the research app still builds | assistant | self-checked | 2026-03-27 | Added `DesktopVoxelLogoLab`, wired a new `voxel` tab in `App.tsx`, built the split-face animation from the real PNG, verified `npm run build`, and confirmed the copied logo asset responds on local `:4007` |
| A-011 | Expand the voxel-logo playground with denser variants, tighter seam handling, improved top/tail sampling, and richer intro/scroll motion, then verify the app still builds | assistant | self-checked | 2026-03-27 | Rebuilt `DesktopVoxelLogoLab` into a 3-variant screen with parameterized voxel density, seam inset, improved coverage sampling, rounded voxel drawing, and optional scroll-responsive drift; `npm run build` passed afterward |
| A-012 | Add two compact cases variants with reduced card scale, no open buttons, and non-button stack tags, then verify the research app still builds | assistant | self-checked | 2026-03-27 | Added `DesktopCasesV3`, `DesktopCasesV4`, and a shared `CompactCaseCards` helper so cases can be shown as smaller grid items with title-first hierarchy and subdued metadata tags; `npm run build` passed afterward |
| A-013 | Add a duplicate of the latest speakers variant, remove the divider line before the description, adapt the grid after review, and verify the research app still builds | assistant | self-checked | 2026-03-27 | Added `DesktopSpeakersV4`, kept the same image/text structure as `V3`, removed the border-top separator before the description, after review switched the latest desktop grid to 5 columns, and verified `npm run build` |
| A-014 | Optimize the dedicated voxel-logo lab after review by keeping only one variant, tightening the seam, and reducing voxel/render cost, then verify the research app still builds | assistant | self-checked | 2026-03-27 | Removed the extra voxel canvases, kept only the densest variant, reduced render cost via lower internal resolution, lower glow, and voxel culling, moved the right half closer to the center seam, and verified `npm run build` |
| A-015 | Relocate the current live-site speakers block into research, add bolded selling keywords to the always-open 5-column variant, and verify the research app still builds | assistant | self-checked | 2026-03-27 | Added `DesktopSpeakersOriginal` plus shared speaker-highlight formatting, wired it into the `speakers` tab, and verified `npm run build` |
| A-016 | Rework the right-eye opening in `DesktopVoxelLogoLab` against the actual logo silhouette, then verify the research app still builds and visually re-check the page | assistant | self-checked | 2026-03-27 | Replaced the old geometric eye mask with PNG-driven dark-region sampling, removed the fake rectangular cutout behavior, preserved more voxels on the outer/top arc to keep the right half rounder, re-ran `npm run build`, and took fresh Playwright screenshots of `#voxel` for visual inspection |
| A-017 | Differentiate one compact `cases` variant with a greener hover state, richer preview graphics, and non-button stack tags, then verify the research app still builds | assistant | self-checked | 2026-03-27 | Reworked `CompactCaseCards` for `variant=\"signal\"`, switched the palette to the site’s green/purple/gray, translated compact card titles into Russian, flattened the tool tags into metadata text, re-ran `npm run build`, and captured a fresh Playwright screenshot of `#cases` for review |
| A-018 | Rework the `pricing` experiment cards after review and verify both desktop and mobile behavior | assistant | self-checked | 2026-03-27 | Updated `DesktopPricingV1` and `labMirrorContent` so the cards use a single rectangular tag, smaller technical list markers, `записаться` CTA, cleaned premium duplication, and a one-column mobile stack; `npm run build` passed and fresh Playwright screenshots were captured for desktop and iPhone 12 |
| A-020 | Rebuild the hybrid `Program NEW` variant on top of the first old sticky program card instead of the long V6 stack, update visible version numbering labels, and expand it into several alternatives, then verify the research app still builds | assistant | implemented | 2026-03-29 | User reported unresolved duplication and styling issues; current pass continues the numbering cleanup and adds a more functional split variant before the next verification pass |
| A-021 | Add two new top-of-list `Program NEW` variants based on the three-card lecture idea and verify they render correctly before review | assistant | implemented | 2026-03-29 | User reported unresolved layout and duplication issues; these variants remain under revision and are no longer treated as fully self-checked |
| A-022 | Add a new top-of-list split `Program NEW` variant that keeps the dark left visual language but uses the sticky left rail, removes decorative duplication, and verify it after build and screenshot review | assistant | implemented | 2026-03-29 | Added `DesktopTechUiV18` and wired it first in the `program_main` list; build and visual verification still pending in this pass |
| A-023 | Reorder the mid-list `Program NEW` entries so the former visible `ВЕРСИЯ 17` and `ВЕРСИЯ 18` become `ВЕРСИЯ 15` and `ВЕРСИЯ 16`, then rebuild the research app | assistant | implemented | 2026-03-30 | Updated the render order and labels inside `src/App.tsx`; build verification pending in this pass |
| A-024 | Restyle `ВЕРСИЯ 13` in `DesktopTechUiV16` so the center dark lecture card gets a richer signal background and the `Недельный ритм` block becomes visibly quieter, then rebuild the research app | assistant | implemented | 2026-03-30 | First pass improved the calendar scale, but the user reported text/art collisions and missing blur-language cues; this pass keeps the calendar size and corrects typography plus the protective composition of the dark card |
| A-025 | Rebuild the lower dark-card system in `ВЕРСИЯ 14` so the cards borrow the blurred neon-poster language, protect text readability, keep the black section, and compress the rhythm block height | assistant | implemented | 2026-03-30 | Current pass further reduces card clipping by raising the lower section and strengthens the central advanced-card blur stack in the `all-neon` branch; verification is still pending until the visual match is accepted |
| A-026 | Tone down the left-side artwork and compress the weekly rhythm area in `ВЕРСИЯ 16` so the dark hero remains readable and the white rhythm block no longer dominates the card | assistant | implemented | 2026-03-30 | Updated `DesktopTechUiV15` with a stronger left-to-right dark gradient, restored the rhythm grid to the `ВЕРСИЯ 13` scale, reduced the overall card height, and changed the layout so the dark hero absorbs leftover height while the white rhythm shelf stays content-sized; verification pending in this pass |
| A-027 | Recompose `ВЕРСИЯ 14` around a shared lower blur field with simpler cards, full weekly copy on the left, and a compact rhythm block on the right, then re-check the local route for review handoff | assistant | implemented | 2026-03-30 | Added `SharedLectureBackdrop` and `OverlayLectureCards`, rewired `DesktopTechUiV17` to a row-aware top grid, reduced the number of visible text styles, widened the rhythm area without squeezing the description, narrowed the overall card shell, increased lecture-card translucency, and confirmed `http://localhost:4007/ai-mindset-blocks/#program_main` still returns `200 OK`; visual browser approval is still pending |
| A-028 | Add a new `speakers` variant derived from version 2 with portrait 2:3 images, text always below, and corner-frame decoration, then surface direct review links | assistant | implemented | 2026-03-30 | Added `DesktopSpeakersV5`, wired it into `App.tsx` as a new speakers version, and preserved the existing page flow so the new variant can be reviewed alongside the previous ones |
| A-029 | Verify the refined right-eye cutout in `DesktopVoxelLogoLab` still compiles and matches the tightened voxel contour | assistant | self-checked | 2026-03-31 | Ran `npm run build` after widening the eye cutout and shifting the stray pixel left; build artifacts show 0 errors and the animation is ready for a visual check |

## Approved / Closed
